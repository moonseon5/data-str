// int [5]형 배열(자료형이 int형이고 요소가 5개)에 값을 입력해 출력
#include <stdio.h>

// #define N 5                 //배열의 요소 개수
int main(void)
{
    /* int foo[N];               //배열의 선언
    for(int i = 0; i < N; i++){    //각 요소에 값을 입력
        printf("foo[%d] :",i);
        scanf("%d",&foo[i]);
    }

    puts("각 요소의 값");
    for(int i = 0; i < N; i++){     //각 요소의 값을 출력
        printf("foo[%d] = %d\n",i,foo[i]);
    }

    return 0; */


    //int형 배열을 초기화하고 출력
    /* int foo[5] = {13, 42, 53, 134, 25};    // ctrl shift L 변수 한번에 수정 F2도 동일
    int foo_size = sizeof(foo) / sizeof(int);  // 요소의 개수
    printf("배열 a의 요소 개수는 %d입니다\n",foo_size);

    for(int i = 0; i < foo_size; i++)
        printf("foo[%d] = %d\n",i,foo[i]);

    return 0; */


   
}